# Stable Auto Generate
### This is a script to randomize prompt and automatically send to generation. 
1. The tags for randomization are added to the prompt.properties file (Path: src/main/resources). 
2. The generation is timed, the generation time can be changed in properties. 
3. I use stable webui AUTOMATIC1111.
